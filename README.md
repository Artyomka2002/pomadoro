    "test": "echo \"Error: no test specified\" && exit 1", - Заменить husky после окончательно версси для test(На данный момент отключен)
     Запустить проект локально ->>
     cd pomadoro -> npm run dev;

     NEXTstart ->> npm run start localhost//:3000